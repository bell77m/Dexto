import { component$ } from '@builder.io/qwik';

export interface GuidePageProps {

}

export const GuidePage = component$<GuidePageProps>((props) => {
  return (
    <div>
      GuidePage component works!
    </div>
  );
});
