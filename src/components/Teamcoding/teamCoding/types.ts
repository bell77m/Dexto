export interface ButtonProps {
  text?: string;
  onClick?: () => void;
}

export interface HeroSectionProps {
  title?: string[];
  description?: string;
  buttonText?: string;
  imageSrc?: string;
  imageAlt?: string;
}
