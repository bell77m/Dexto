import { component$ } from '@builder.io/qwik';

export interface AboutPageProps {

}

export const AboutPage = component$<AboutPageProps>((props) => {
  return (
    <div>
      AboutPage component works!
    </div>
  );
});
